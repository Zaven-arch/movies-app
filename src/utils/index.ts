import { validateMovieInput } from './validate-movie-input.util'
import { $getColor } from './get-color.util'
import { $toCapitalize } from './to-capitalize.util'

export { validateMovieInput as $validateMovieInput, $getColor, $toCapitalize }
